# Application Mode: 'local' (default) or 'remote'
APP_MODE=local

# Google Cloud Project ID (Required for Remote Mode)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud Storage Bucket for Audio (Required for Remote Mode)
GCS_BUCKET_NAME=your-gcs-bucket-name

# Firestore Collection for Metadata (Default: 'generations')
FIRESTORE_COLLECTION=generations

# Path to Service Account JSON (Optional, if not using ADC)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
